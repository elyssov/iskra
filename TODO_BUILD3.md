# TODO для Build 3
## Составлено Ларой-3, 6 апреля 2026

### КРИТИЧНЫЕ (до деплоя)

1. **Деплой relay v0.3 на Render** — hold persistence готов, не задеплоен
2. **Тест доставки сообщений** — тестеры жалуются что сообщения не доходят. Возможно relay на Render перезапускается и теряет pending. Новый hold это чинит.
3. **Проверить Павла (Samsung A22)** — useLegacyPackaging добавлен, нужен тест

### ВАЖНЫЕ

4. **Поиск в контактах** — поле `#search-input` есть в HTML, но без обработчика в JS
5. **Кнопка "Новый чат"** (`#btn-new-chat`) — есть в HTML, нет обработчика
6. **Кнопка "Скан QR"** (`#btn-scan-qr`) — есть в HTML, нет обработчика (нужен Android Intent)
7. **Display name** — input `#settings-display-name` без обработчика (не сохраняет/отправляет)
8. **Кнопка "24 слова"** (`#btn-show-mnemonic`) — нет обработчика в settings
9. **Reply-to в личных чатах** — работает в группах, не реализовано в личных

### RELAY

10. **Relay hold: saveHold при каждом сообщении** — может тормозить при высоком трафике. Можно debounce (сохранять раз в 5 сек).
11. **Relay hold: concurrent write** — saveHold вызывается из разных горутин без синхронизации записи файла. Нужен mutex или channel.

### ПОЧТА

12. **Входящие письма: fromName не резолвится** — приходит userID, а не имя контакта. Нужно резолвить на клиенте через contacts.
13. **Письма не загружаются при переключении на вкладку Почта** — `renderMailList()` делает `loadLetters().then(renderMailFolders)`, но первый вызов может быть до авторизации PIN.
14. **Compose: "Кому" — после выбора из dropdown, при повторном редактировании selectedUid не сбрасывается**

### ИЗ СКАНИРОВАНИЯ КОДА

18. **relay: saveHold() игнорирует ошибку записи** — os.WriteFile без проверки, при ошибке диска данные молча теряются
19. **relay: holdMaxTotal не проверяется** — только per-user лимит (200), глобальный (50000) не enforcen
20. **app.js: fetch без проверки r.ok** — минимум 10 мест где .json() вызывается без проверки HTTP статуса. Если сервер вернёт ошибку — парсим мусор
21. **app.js: race condition в loadMessages/loadGroupMessages** — currentContact может измениться между проверкой и fetch

### МЕЛОЧИ

15. **i18n** — новые строки почты не переведены (mail_empty_title, letter_fill, letter_sent и т.д.)
16. **Help modal** — пустой (`#help-content` без содержимого)
17. **Toast** — создаётся динамически каждый раз, можно переиспользовать

---
*Лара-3, перед сном. Завтра — деплой relay и Build 3.*
